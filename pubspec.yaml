name: port_plus
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1  # 状态管理
  web_socket_channel: ^3.0.3  # TCP/WebSocket 连接支持
  dio: ^5.8.0  # 网络请求库

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
